<div id="home-header">
    <div class="row">
        <div class="col-md-3 col-sm-12"><h1>Logo Sortir.com</h1></div>
        <div class="col-md-9 col-sm-12 text-right">
            <a href="{{ path('city') }}" class="link col-md-2 menu_link">Villes</a>
            <a href="{{ path('site') }}" class="link col-md-2 menu_link">Sites</a>
            <a href="{{ path('home') }}" class="link col-md-2 menu_link">Accueil</a>
            <a href="{{ path('profil') }}" class="link col-md-2 menu_link">Mon Profil</a>
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('app_register') }}" class="link col-md-2 menu_link">Créer un utilisateur</a>
            {% endif %}
            <a href="{{ path('app_logout') }}" class="link col-md-2 menu_link">Se déconnecter</a>
        </div>
    </div>
</div>